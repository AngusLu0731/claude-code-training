---
title: Claude Code 進階實作教材
description: 給已會 Claude Code 基礎的人：多 session 多工、workflow 定義、subagent、hooks、skills，五章跟著做，最後一份期末作業把全部用上。
head:
  - tag: title
    content: Claude Code 進階實作教材
---

import { Steps, Aside, Tabs, TabItem } from '@astrojs/starlight/components';
import Progress from '../../components/Progress.astro';
import OrcaLauncherDemo from '../../components/diagrams/OrcaLauncherDemo.astro';
import OrcaAgentDemo from '../../components/diagrams/OrcaAgentDemo.astro';
import { CLAUDE_CODE_VERSION, VERIFIED_ON } from '../../data/version';

<div class="home-hero">
	<p class="home-tagline">
		五個進階主題加一份期末作業，跟著做、看到結果、勾掉自檢。
		<span class="home-topics">multi-session ・ workflow ・ subagent ・ hooks ・ skills</span>
	</p>
	<div class="home-actions">
		<a class="home-cta" href="/01-skills/">開始第 1 章 →</a>
		<a class="home-cta secondary" href="#開始前">先看開始前檢查</a>
	</div>
</div>

這是一套「跟著做」的教材，不是講義。五個進階主題各自成章，每章固定四段：**用途**、**什麼時候用**、**基本用法**、**你來做**。
「你來做」給你可以直接複製的指令、做完應該看到什麼、以及自檢勾選；真正的練習在你自己的終端機裡完成，網頁只負責帶路與記錄進度。
五章之後有一份**期末作業**：不再給檔案貼，只給規格，你自己寫工具、交付一個版本，最後由驗收腳本告訴你過了沒。

<Progress />

## 開始前

**對象**：已會 Claude Code 基礎的人——會在終端機開 `claude`、會下提示詞、看得懂 `git diff`。沒碰過 worktree、workflow、subagent、hooks、skills 也沒關係，這五章就是為此而寫。

### 檢查清單

- `claude --version` 顯示 **{CLAUDE_CODE_VERSION} 或更新**；比較舊就先 `claude update`。各章行為在幾個版本之間有分水嶺，版本太舊會跟站上寫的對不起來。
- `python3 --version` 顯示 **3.9 或更新**（練習 repo 只用標準函式庫，不用裝套件）。
- 有 `git`。

### 會用到、你可能沒見過的名詞

五章會反覆用到下面這些字，第一次看到卡住就回來查這張表：

- **`origin/main`**：GitHub 上那份 `main` 在你電腦裡的複本（clone 時抓下來的）。各章重置指令的 `git reset --hard origin/main`＝把 repo 弄成跟 GitHub 一模一樣。
- **fast-forward／`--no-ff`**：git 合併分支時，如果 `main` 沒有新 commit，預設會直接把 `main` 的指標挪到分支尾端（fast-forward），不留任何合併紀錄；`--no-ff` 強制留一個合併 commit，之後才看得出哪批改動是誰做的。
- **tag**：給某個 commit 貼一個名字，例如 `v1.0.0`。
- **heredoc（`cat > 檔名 <<'EOF' … EOF`）**：把兩個 `EOF` 之間的內容一字不改寫進那個檔案。整段一起複製、貼到終端機、按 Enter；提示符回來、沒有錯誤就是寫好了。
- **stdin／stderr／exit code**：餵給程式的輸入／程式印錯誤訊息用的管道／程式結束時回報的數字（`0`＝正常）。第 4 章的 hook 就靠這三樣跟 Claude Code 溝通。
- **環境變數**：shell 傳給程式的鍵值，例如 `TODO_FILE=/tmp/x.json python3 src/todo.py list`。
- **`git status --porcelain`**：精簡格式的 `git status`：`??`＝新檔、還沒被 git 追蹤；` M`＝已追蹤的檔被改了。各章「預期看到」拿它當證據。
- **token／context**：token 是內容量的計量單位；context 是 Claude 當下記得的所有內容（這段對話裡讀過的檔、講過的話）。留在 context 裡的東西，之後每一句對話都要重新送一次——第 2 章會用數字算給你看。

### 選一條路：CLI 或 Orca

全站的操作步驟提供兩條等價的路，帶分頁的區塊點一下就能切換；選擇會記住並同步到全站的所有分頁：

- **CLI**：直接在系統終端機開 `claude`。什麼都不用多裝。
- **Orca**：在 [Orca](https://www.onorca.dev/)（開源的 Agent Development Environment，桌面應用）裡跑 Claude Code。它的核心概念是「一個任務一個 git worktree」（worktree＝repo 的一份獨立工作目錄，第 5 章才會教，現在知道這句就好），每個 worktree 有自己的終端機與 agent——到第 5 章你會發現它把整章要手動做的事變成了 UI。

兩條路跑的是**同一個 Claude Code**：Orca 直接沿用 `~/.claude` 的登入與設定，`.claude/` 專案檔、`/指令`、subagent、hooks 全部相同。所以只有「怎麼開出一個 session、怎麼重置起點」的區塊有分頁；提示詞、檔案內容、`/指令` 兩條路共用。

<Tabs syncKey="tool">
<TabItem label="CLI">

不用額外安裝。在終端機 `cd` 到 repo、打 `claude`，就是一個 session。

</TabItem>
<TabItem label="Orca">

先裝 Orca（macOS 用 Homebrew；其他平台到[下載頁](https://www.onorca.dev/download)），前提是 `claude` CLI 已裝好並登入過一次：

```bash
brew install --cask stablyai/orca/orca
```

第一次啟動會要求家目錄的存取權（用來加 repo），並提議匯入既有的 `~/.claude` 設定——同意即可，Claude Code 的登入直接沿用。之後的固定模式：側欄 **Add Repo** 加入本機 checkout → 按 repo 名旁的 **＋** 開一個 worktree（輸入任務名，起點用預設的 `main`——本機的分支）→ agent 下拉選 **Claude Code**，Orca 會自動啟動它。進到 worktree 之後，提示詞、檔案內容、`/指令` 都跟 CLI 相同；要下 shell 指令（貼檔案）就按 **Cmd-T** 在同一個 worktree 開新終端機分頁，agent 結束後按分頁上的 **Restart** 重開。

<OrcaLauncherDemo>
每章 Orca 路線的固定開場：① 按 repo 旁的 ＋ → ② 取任務名、起點用預設的 main → ③ agent 選 Claude Code。UI 文字以你裝到的版本為準。
</OrcaLauncherDemo>

</TabItem>
</Tabs>

各章「起點」的重置也依路線不同：CLI 用 reset 指令把 repo 洗回乾淨的 `main`；Orca 直接**開一個新 worktree**——它本來就從乾淨的 main 長出來，重做一章＝刪掉舊 worktree、再開一個新的。

### 四件先講清楚的事

<Aside type="caution" title="請在終端機執行 claude">
本站所有步驟都在終端機執行 `claude`——直接開系統終端機（CLI 路線），或用 Orca 內建的終端機（Orca 路線）都可以，兩者畫面一致。VS Code 或 Desktop 使用者請開一個終端機照做；IDE 面板的按鈕與提示文字不同，容易對不上。
</Aside>

<Aside type="note" title="權限模式不同，提示就不同">
auto／manual 權限模式看到的提示不一樣（有人會被問「要不要允許」，有人不會）。站上「預期看到」一律以**結果**為準：`/workflows` 有沒有進度、`git status` 有沒有那個檔、diff 是不是那一行。提示長得不一樣不代表做錯。在 `claude` 裡打 `/permissions` 可以看你目前的模式；本教材不要求切換，出現「要不要允許」就選允許。
</Aside>

<Aside type="tip" title="進度只存在你的瀏覽器">
自檢勾選存在這台電腦的瀏覽器（localStorage），不會上傳、換電腦不會同步。Safari 超過 7 天沒開本站會把它清掉，建議用 Chrome，或一週內做完。
</Aside>

<Aside type="caution" title="常常需要兩個終端機">
從第 4 章起，很多步驟要你在 `claude` 開著的同時「在終端機」下 git 指令或貼檔案。請習慣開兩個以上的視窗、都 `cd` 到 repo：一個跑 `claude`、一個跑 shell（第 5 章與期末會用到三個）。或者在 `claude` 的提示符最前面打 `!`（例如 `!git status`），Claude Code 會直接執行那行 shell 指令、不交給 Claude——之後各章寫「`!ls …`」就是這個意思。
</Aside>

### 共用步驟（每一章都從這裡開始）

<Steps>

1. 把練習 repo clone 下來，之後五章都在這個目錄裡做（Orca 路線也一樣需要這份本機 checkout）：

   ```bash
   git clone https://github.com/AngusLu0731/claude-code-practice.git
   cd claude-code-practice
   ```

2. 開出第一個 session，接受信任對話：

   <Tabs syncKey="tool">
   <TabItem label="CLI">

   第一次在這個目錄開 `claude`，會出現「要不要信任這個資料夾」的對話，選 **Yes**：

   ```bash
   claude
   ```

   </TabItem>
   <TabItem label="Orca">

   開 Orca，側欄按 **Add Repo** 選剛 clone 的 `claude-code-practice`，再按 repo 名旁的 **＋** 開一個 worktree（名字隨意，例如 `hello`），agent 選 **Claude Code**——Orca 會自動啟動它，不用自己打 `claude`。終端機裡出現「要不要信任這個資料夾」的對話，選 **Yes**。

   <OrcaAgentDemo>
   worktree 一開，Orca 就自動啟動 Claude Code——信任對話直接出現在終端機裡，畫面跟 CLI 路線一致。
   </OrcaAgentDemo>

   </TabItem>
   </Tabs>

3. 看到提示符就可以打 `/exit` 離開。CLI 路線信任只要接受這一次，之後重開不會再問；Orca 路線每個新 worktree 是獨立目錄，第一次進去會再被問一次，一樣選 Yes 就好（這個試水用的 worktree 之後可以在側欄右鍵刪掉）。

</Steps>

## 五章＋期末作業

<div class="chapter-cards">
	<a class="chapter-card" href="/01-skills/">
		<span class="chapter-num">01</span>
		<span class="chapter-name">skills</span>
		<span class="chapter-desc">一份步驟說明書：自己的 changelog skill，用 /changelog 叫起來</span>
	</a>
	<a class="chapter-card" href="/02-subagent/">
		<span class="chapter-num">02</span>
		<span class="chapter-name">subagent</span>
		<span class="chapter-desc">一個獨立執行者：自己的 reviewer.md，派它審一個檔</span>
	</a>
	<a class="chapter-card" href="/03-workflow/">
		<span class="chapter-num">03</span>
		<span class="chapter-name">workflow 定義</span>
		<span class="chapter-desc">一條產線編排多個 subagent：review-flow.js，用 /review-flow 跑完</span>
	</a>
	<a class="chapter-card" href="/04-hooks/">
		<span class="chapter-num">04</span>
		<span class="chapter-name">hooks</span>
		<span class="chapter-desc">把規則變成閘門：一個 PreToolUse hook，親眼看到它擋下一次 Edit</span>
	</a>
	<a class="chapter-card" href="/05-multi-session/">
		<span class="chapter-num">05</span>
		<span class="chapter-name">多 session 多工</span>
		<span class="chapter-desc">兩個 worktree 分工、第三個終端機 merge 收斂</span>
	</a>
	<a class="chapter-card capstone" href="/06-capstone/">
		<span class="chapter-num">期末</span>
		<span class="chapter-name">作業：交付 v1.0.0</span>
		<span class="chapter-desc">自己寫 hook、skill、subagent、workflow，兩個 worktree 平行，出一個版本，驗收腳本印 CAPSTONE_OK</span>
	</a>
</div>

前三章是一條遞進鏈——**說明書（skill）→ 執行者（subagent）→ 產線（workflow）**；第 4 章把規則變成閘門，第 5 章把多個 session 平行起來。每章大約 20 到 40 分鐘，建議照章走；不過每章開頭都會把 repo 重置回乾淨狀態，跳著做也可以（第 5 章與期末的重置多幾行，見各章）。期末作業約 60 到 90 分鐘，五章都做過再來——它把五個工具串成一條真正的交付流程，而且工具檔要你自己寫。

### 我的問題該用哪個工具？

先給一張地圖，之後每章都會回來對照：

| 你的情況 | 用什麼 | 在哪章 |
| --- | --- | --- |
| 每次 session 都該知道的事實與規範（指令、風格、慣例） | `CLAUDE.md` | [官方文件](https://code.claude.com/docs/en/memory) |
| 一套會重複執行的步驟（程序） | skill | 第 1 章 |
| 要讀很多東西、只要結論；或要一個固定角色 | subagent | 第 2 章 |
| 同一套多 agent 流程，每次都要長一樣 | workflow | 第 3 章 |
| 沒有例外的硬規則（不准改、每次必跑） | hook | 第 4 章 |
| 兩件不相干的事要同時進行 | 多 session＋worktree | 第 5 章 |
| 把上面五個串成一條交付流程，確認自己真的會了 | 全部 | 期末作業 |

口訣：**事實進 CLAUDE.md、程序進 skill、隔離用 subagent、編排用 workflow、強制用 hook、平行開 session**。

<Aside type="note">
教材內容以 Claude Code {CLAUDE_CODE_VERSION}（{VERIFIED_ON} 實跑驗證）為準。之後版本若行為不同，以官方文件為主：
[sub-agents](https://code.claude.com/docs/en/sub-agents)、[hooks](https://code.claude.com/docs/en/hooks)、[skills](https://code.claude.com/docs/en/skills)、[workflows](https://code.claude.com/docs/en/workflows)、[worktrees](https://code.claude.com/docs/en/worktrees)。
</Aside>
